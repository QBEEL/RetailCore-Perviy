PySide6>=6.10
qtawesome>=1.3
rapidfuzz>=3.9
openpyxl>=3.1

# Доступ к КриптоПро для подписи запросов в Честный ЗНАК. Только Windows:
# на других системах КриптоПро нет, и подпись там всё равно невозможна.
pywin32>=306; platform_system == "Windows"

# Корневые сертификаты для HTTPS. На macOS системного хранилища для
# Python нет, и в собранном приложении проверка сертификата сервера
# падает без этого набора.
certifi>=2024.2
